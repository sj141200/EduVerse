
# Example environment variables
# If REACT_APP_API_URL is empty, the app runs in MOCK mode (no real backend calls).
REACT_APP_API_URL=http://localhost:5000

# Optional: direct Azure Blob SAS base URL for uploads (e.g. https://<account>.blob.core.windows.net/<container>?<sas_token>)
REACT_APP_BLOB_SAS=
